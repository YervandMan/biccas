{
  "name": "biccas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx tailwind -i ./styles.css -o ./dist/styles.css --watch",
    "build:css": "npx tailwindcss -i ./styles.css -o ./dist/styles.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tailwindcss": "^3.4.3"
  }
}
